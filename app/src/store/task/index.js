const state = {
    tasks: []
}

const actions = {
    
}

const mutations = {
}

const getters = {
   
}

const tasks = {
    namespaced: true,
    state,
    actions,
    mutations,
    getters
}

export default tasks